<Window x:Class="ABBDataManagerSystem.Pages.TempSlotSelectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ABBDataManagerSystem.Pages"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="温度测试槽位选择" Height="720" Width="1080" ShowInTaskbar="False" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="hc:NumericUpDown" BasedOn="{StaticResource GlobalNumericUpDownStyle}">
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style TargetType="hc:ComboBox" BasedOn="{StaticResource GlobalComboBoxStyle}">
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="IsEnabled" Value="{Binding IsActive}"/>
            <Setter Property="Opacity" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.45"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="通道数量" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <hc:NumericUpDown Name="nudChannelCount" Width="100" Minimum="0" Maximum="27" ValueChanged="nudChannelCount_ValueChanged"/>
            <TextBlock Text="槽位上限" VerticalAlignment="Center" FontSize="16" Margin="18,0,10,0"/>
            <TextBlock Name="tbMaxSlot" VerticalAlignment="Center" FontSize="16"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  Name="dgChannels"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  RowHeight="32"
                  ColumnHeaderHeight="34">
            <DataGrid.Columns>
                <DataGridTextColumn Header="序号" Binding="{Binding RowIndex}" IsReadOnly="True" Width="70"/>
                <DataGridTextColumn Header="通道名称" Binding="{Binding RoleName}" IsReadOnly="True" Width="160" Visibility="Collapsed"/>
                <DataGridTextColumn Header="通道标题" Binding="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTemplateColumn Header="温度探头" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Probe}" TextAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <hc:ComboBox ItemsSource="{Binding DataContext.SlotOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                         SelectedItem="{Binding Probe, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         hc:InfoElement.ShowClearButton="True"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsActive}" IsReadOnly="True" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
            <Button Content="确认" Style="{StaticResource ButtonPrimary}" Width="90" Height="36" FontSize="16" Margin="0,0,16,0" Name="btConfirm" Click="btConfirm_Click"/>
            <Button Content="取消" Style="{StaticResource ButtonPrimary}" Width="90" Height="36" FontSize="16" Name="btCancel" Click="btCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
