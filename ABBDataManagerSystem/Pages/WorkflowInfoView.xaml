<Window x:Class="ABBDataManagerSystem.Pages.WorkflowInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ABBDataManagerSystem.Pages"
        mc:Ignorable="d"
        Title="工作令列表" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="请选择工作令" FontSize="24" Margin="0, 10"/>
        <DataGrid Name="dataGrid" Grid.Row="1"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <Button Name="btConfirm" Content="确认" Click="btConfirm_Click" Style="{StaticResource ButtonPrimary}"/>
            <Button Name="btCancel" Content="取消" Click="btCancel_Click" Style="{StaticResource ButtonPrimary}" Margin="5,0"/>
        </StackPanel>
    </Grid>
</Window>
