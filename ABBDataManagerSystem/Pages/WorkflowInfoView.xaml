<Window x:Class="ABBDataManagerSystem.Pages.WorkflowInfoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ABBDataManagerSystem.Pages"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="工作令列表" MinHeight="800" MinWidth="1200" Loaded="Window_Loaded" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="请选择工作令" FontSize="24" Margin="0, 10"/>
        <DockPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right">
            <hc:SearchBar Name="SearchBar" Width="400" hc:InfoElement.Title="工作令搜索" Style="{StaticResource SearchBarPlus}" hc:InfoElement.TitlePlacement="Left" FontSize="18" BorderBrush="Gray" SearchStarted="SearchBar_SearchStarted"/>
        </DockPanel>
        <DataGrid Name="dataGrid" Grid.Row="1" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserResizeColumns="False"
                  CanUserResizeRows="False" CanUserSortColumns="False" IsReadOnly="True" SelectionMode="Single" MouseDoubleClick="dataGrid_MouseDoubleClick"/>
        <Grid Grid.Row="2" Margin="0, 20, 0, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="580"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <Button Width="90" FontSize="18" Height="40" Name="btConfirm" Content="确认" Click="btConfirm_Click" Style="{StaticResource ButtonPrimary}"/>
                <Button Width="90" FontSize="18" Height="40" Name="btCancel" Content="取消" Click="btCancel_Click" Style="{StaticResource ButtonPrimary}" Margin="5,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <TextBlock Text="共120页，当前第1页" Width="200" VerticalAlignment="Center" TextAlignment="Right" Name="tbPageInfo"  FontSize="18" />
                <Button FontSize="18" Name="btFirst" Content="首页" Click="btFirst_Click" Style="{StaticResource ButtonPrimary}" Height="40" Margin="5, 0, 2, 0" Width="90"/>
                <Button FontSize="18" Name="btPrev" Content="上一页" Click="btPrev_Click" Style="{StaticResource ButtonPrimary}" Height="40" Margin="2, 0" Width="90"/>
                <Button FontSize="18" Name="btNext" Content="下一页" Click="btNext_Click" Style="{StaticResource ButtonPrimary}" Height="40" Margin="2,0" Width="90"/>
                <Button FontSize="18" Name="btLast" Content="末页" Click="btLast_Click" Style="{StaticResource ButtonPrimary}" Height="40" Margin="2,0" Width="90"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
